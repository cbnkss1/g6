import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "게임머니 전환 · SLOTPASS",
  description: "게임머니와 Plxmed 카지노 지갑 간 이동",
};

export default function GameMoneyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
